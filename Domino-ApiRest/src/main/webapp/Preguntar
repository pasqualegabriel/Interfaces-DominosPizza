1) como importar todos los objetos de modelo, los metemos todos en un solo file, o los declaramos uno a uno en el index?

2) Para el usuario, usamos un constructor para hidratar el json que se recibe del API, y otro para utilizar como estructura.
Esta bien que haya dos?

3) No hay mucha responsabilidad en los controllers de armar y manejar los pedidos? no nos estaria haciendo falta un objeto/funcion constructor de pedidos que maneje
toda esa logica?



        var plato = platoService.newPlato(new Pizza("Customizada",70));
        self.pedido.addPlato(plato);
        self.pedido.setIdPizzaActual(unaPizza.nombre);
        $state.go("seleccionDeTamanio",{nombre:pizzaService.newPizza()});

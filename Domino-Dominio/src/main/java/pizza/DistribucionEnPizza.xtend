package pizza

/**
 * Clase del tipo Enum que se encarga de representar la distribucion de un ingrediente en una pizza.
 * Esta distribucion puede ser "Izquierda", "Derecha" o "Toda" la pizza
 */
enum DistribucionEnPizza 
{
	Toda, Izquierda, Derecha
}
